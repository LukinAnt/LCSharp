// переменная Н, счетчик i; создаем цикл while; добавляем счетчик; в строку второй вывод; 
// третий вывод с пробелом в одинарных кавычках но второй нужно оставить

int n = 100;
int i = 1;

while (i <= n)
{
        //Console.WriteLine(i);
        Console.Write(i);
        Console.Write(' ');
        i = i + 1;
}
